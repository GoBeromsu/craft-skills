# Changelog

- 2026-06-16 — v3.1.1 recipe-law prose hygiene (Layer-2 consensus follow-up). Removed the capability-blurb tail from the `description` frontmatter so it carries only trigger phrases (every trigger phrase preserved — no contract change, hence PATCH). Purged unexplained terms of art the skill's own jargon gate forbids: `SSOT` → "single source of truth" on first use; `OMC-independent` → "orchestrator-independent" with the dependency stated in plain language ("no dependency on any agent-orchestration runtime"). Split the Governance scope ambiguity the consensus panel flagged: the break-glass protection applies to the skillify package itself (`skills/skillify/**`), while ordinary lifecycle work skillify performs on other skills follows the normal branch → commit → PR flow without break-glass approval. No behavioral change.
- 2026-06-16 — v3.1.0 add Stage 0: Admission — a pre-Harvest scope gate distinct from Layer 1 (format) and Layer 2 (trust) that answers "does this candidate belong in craft-skills at all?" before any authoring cost. New `references/checklist.md` holds the admission rubric: five drop-questions (Q1 Reusability, Q2 Ownership, Q3 Convention-not-artifact, Q4 Portability, Q5 Boundary-purity), each defaulting to ✗ with a routed fail-action (project-local / using-our-stack reference / split-then-resubmit) and a result-routing rule that records where/why a rejected candidate landed so it is not re-litigated. Generalized `agents/reviewer.md` from a quality-only SKILL-draft auditor into a checklist-driven impartial judge with two modes — admission (pre-harvest, fed checklist.md, never the author's promotion rationale) and quality (post-author anatomy axes) — MECE-separated by which rubric it is handed and when invoked, not by two overlapping charters. Added a `§ Admission Task` template to `references/pipeline.md` (single clean pass, fresh subagent, never the consensus loop). Wired a Tier-1 entry-gate precondition (admission receipt verdict=ADMIT); on REJECT the author has no veto — disagreement escalates to the human. The admission receipt is a runtime log under the gitignored `evals/`, keyed by candidate slug since a rejected candidate may have no skill directory yet.
- 2026-06-13 — v3.0.4 vendored into craft-skills as the self-governing promotion gate. Genericized every bstack/Hermes/Ataraxia reference across SKILL.md, references/, scripts/, and tests/ to craft-skills + Claude Code/Codex. Fixed residual jargon and leaks the genericize pass left behind: `합일`→`CONVERGED` in the Layer-2 convergence loop, an inline definition added for `load key`, the Korean colloquial `우리끼리만`→plain-English `local-only` condition, removal of `hermes` from the valid `compatibility` values and routing Compatibility column in `references/schemas.md`, and the orphaned `vault skill` hand-off line generalized to the operator's note-taking workflow.
- 2026-06-07 — v3.0.2 runtime-hygiene + portability fix for the Hermes hook command: the command in `.hermes/hooks/protect-skillify-hermes.yaml` (and its echoes in AGENTS.md and this CHANGELOG) was a literal host-absolute home path (`/Users/<you>/…`), which skillify's own `validate-runtime-hygiene.py` (RUNTIME_HOME_PATH rule) correctly flags as a committed host-specific path — CI's `--diff-base` runtime-hygiene gate failed on it. Replaced with the home-relative `~/dev/GoBeromsu/bstack/.claude/hooks/protect-skillify.sh` as the bare (executable, `+x`) script path with NO `bash` prefix: Hermes dispatches via `shlex.split(os.path.expanduser(command))` under `shell=False` (verified in `agent/shell_hooks.py`), so a leading `~` expands to `$HOME` while `${VARS}` do not — and all three hosts (m1-pro Sari/Xia, m3 Goun) check bstack out at the same `~/dev/GoBeromsu/bstack`, so the path is portable with zero per-host substitution. The CC (`${CLAUDE_PROJECT_DIR}`) and Codex (`${PWD}`) registrations were already env-indirected and clean. Also renamed the test-only local `token`→`grant` in `tests/test_protect_skillify.py` so the presence-only `SKILLIFY_EDIT_TOKEN` fixture (the hook checks `[[ -n … ]]`, the value is irrelevant) stops tripping the SECRET_ASSIGNMENT rule.
- 2026-06-07 — v3.0.2 round-2 hardening (consensus REVISE resolution): all three models (codex/gemini/claude) flagged Bash/terminal bypasses in round-1; resolution = (1) broadened the terminal denylist to cover command chaining (`cd skills/skillify && rm -rf *` via rule B), additional mutating verbs (`truncate`, `dd`, `ln`, `chmod`, `chown`, `touch`, `mkdir`, `install`, `rsync`, `find -delete/-exec`, `perl -i`, `python3 -c`, `git checkout/restore/apply`), and a parent-dir safety net (`rm -rf skills` via rule C); (2) path normalization (`//` and `/./` collapsed) in both the write/patch target and the Bash/terminal command before matching; (3) null-payload guard — replaced `d.get('tool_input',{})` with `(d.get('tool_input') or {})` in both python one-liners so `{"tool_input":null}` exits 0 instead of crashing; (4) honest best-effort/defense-in-depth framing added to script header, deny box, and docs — the file-tool block (Write/Edit/write_file/patch) is named as the real enforcement boundary; the Bash/terminal layer is documented as best-effort (interpreter one-liners remain evadable by design); (5) accurate token-scope wording — protection is lifted while `SKILLIFY_EDIT_TOKEN` is set (not "one operation"), with an explicit note to unset immediately; (6) `SKILLIFY_EDIT_TOKEN` headless-Hermes scope asymmetry documented in script header, deny box, AGENTS.md §8, and protect-skillify-hermes.yaml — exporting in a shell does not propagate to a launchd gateway; three operator paths stated; (7) `--accept-hooks`/`hooks_auto_accept` verification claim marked as verified against installed Hermes source; regression suite expanded from 23 to 40 cases.
- 2026-06-07 — v3.0.2 Hermes protection hook made functional: corrected event key `pre_tool_use`→`pre_tool_call`; matcher and script extended to Hermes tool names `terminal`/`write_file`/`patch` alongside Claude Code names `Bash`/`Write`/`Edit`; path extraction now checks `tool_input.file_path` then `tool_input.path` (Hermes uses `.path`); deny output is now dual-runtime — stdout `{"decision":"block","reason":"..."}` (Hermes protocol) plus exit 2 (Claude Code/Codex protocol); home-relative script path `~/dev/GoBeromsu/bstack/.claude/hooks/protect-skillify.sh` used as the bare +x script command (Hermes expands a leading `~` via os.path.expanduser under shell=False; ${BSTACK_REPO_PATH} is not defined at hook-dispatch time and `${VARS}` are not expanded); Bash/terminal deny regex broadened to also catch `>`/`>>` redirection, `tee`, and `sed -i` mutations targeting `skills/skillify`; documented the allowlist registration step (`hooks_auto_accept: true` in config.yaml — the `--accept-hooks` flag is `gateway run`-only and is NOT accepted by `gateway restart`) and three config.yaml targets in `.hermes/hooks/protect-skillify-hermes.yaml` and AGENTS.md §8. Caught dogfooding skillify's own MECE/robustness review against the installed Hermes source contract. Added `tests/test_protect_skillify.py` (23 cases) to lock in the dual-runtime block/allow/token-lift contract so future hook drift is caught by CI before it ships.
- 2026-06-07 — v2.0.0 MECE refactor: adopt addyosmani skill-anatomy as the canonical format SSOT (`references/skill-anatomy.md`); rewrite SKILL.md to the anatomy section flow; drop all `make-skill` and vault-stub/`agent_skill_scope` language; add flat-first placement policy; require a per-skill `.env` for secrets (commit only `.env.example`).
- 2026-06-07 — make skill-format verification a deterministic script, not a prompt: add `scripts/validate-skill-format.py` (+ `tests/test_validate_skill_format.py`) enforcing frontmatter name/description/version, per-package CHANGELOG with a dated bullet, no `## Change Log` in SKILL.md, and no committed real `.env`.
- 2026-06-07 — wire skill-format + runtime-hygiene validators into GitHub CI in `--diff-base` mode (only PR-changed packages are enforced); fix stale `skills/infra/skillify` CI paths to `skills/skillify`.
- 2026-06-07 — consolidate references to MECE (11→8): merge area-folder-migration/area-folder-hygiene/resolver-routing-audit into `topology-and-routing.md`; fold runtime-hygiene-diff-mode into the PR playbook; fold session-review policy into SKILL.md; rewrite `pipeline.md` onto the CHANGELOG.md model.
- 2026-06-06 — clarify backfill discipline for skill edits made before PR flow and prefer follow-up CI fix commits on PR branches over force-push unless explicitly approved.
- 2026-06-05 — record Beomsu's preferred skillify loop: focused branch/PR, return to `main`, Xia review/merge, then pull `main` before the next skill change.
- 2026-06-05 — fix runtime hygiene diff mode to validate the current worktree against the compare base, preventing stale `HEAD` findings after local host-path cleanup.
- 2026-06-03 — make skill governance reviewable; skillify now owns skill CRUD and requires every skill change to go through branch/commit/PR with a package changelog bullet.
- 2026-06-03 — prevent runtime hygiene regressions; linked the runtime hygiene PR playbook from the main skillify operational notes.
- 2026-06-03 — treat user session corrections as skill-update signals; added session review/update mode so workflow corrections patch the governing skill package instead of remaining chat-only.
- 2026-06-06 — two-vault routing migration; replaced bare `Ataraxia/` prefix with `${ATARAXIA_VAULT_PATH}` in both Writer and Reviewer task Skill Guideline paths in pipeline.md.
- 2026-06-07 — v3.0.0 robustness hardening: two-layer verification (Layer-1 deterministic format gate + Layer-2 vendored OMC-independent consensus convergence loop via scripts/consensus.py); 5-key frontmatter (added allowed-tools, compatibility); recipe-completeness law embedded with Common Rationalizations and Red Flags rows; 3-agent roster (writer/reviewer/grader) with explicit author-vs-eval lane separation; protection governance pointer to AGENTS.md §8; all skill-anatomy.md references repointed to references/schemas.md.
- 2026-06-07 — review-driven hardening pass: description reordered to trigger-phrase-first per schemas.md §2 field rule; convergence-loop rebuttal documented as a separate --round 2 --prior invocation (not auto-rebuttal); degraded-run Tier-2 policy stated explicitly (degraded status does not satisfy gate); Layer-1-first clarified as first verification action, not pre-branch; claude consensus role-prompt corrected to not assert an unobservable CHANGELOG.md check; schemas.md §4 completed with CHANGELOG.md, evals/, and agents/ rows (agents/ marked as real defined package part); ARCHITECTURE.md convergence-loop step 4 updated to require second invocation; AGENTS.md §8 Hermes row marked conditional on manual config.yaml merge with verification note; protect-skillify.sh token changed to openssl rand -hex 16 (unguessable); cp added to blocked Bash commands alongside rm/git rm/mv.
- 2026-06-07 — round-3 consensus follow-up: declared `agents/` as a named package part in SKILL.md Overview anatomy list and schemas.md §4, resolving the recurring "unresolved agents/ references" finding across codex r1–r3 and gemini r3; documented `agents/`-over-`prompts/` as a deliberate skill-creator convention choice in the §4 `agents/` row; removed the "before anything else" framing from the Layer-1 heading and description, making it unambiguous that Layer 1 is the first verification action (after branch/commit/PR setup, before Layer 2); disambiguated script-level graceful degradation (script resilience — receipt always written, no hard-crash) from the Tier-2 gate verdict (a degraded run does not pass the gate regardless of script completion).
- 2026-06-07 — round-4 clarity pass for consensus convergence: technical header "The 3 stages: Harvest → Prove → Cement" replaces the cultural "Garry Tan / bstack" framing; inline definitions added at first use for "load key" (the dispatch key Hermes resolves a skill by at routing time) and "thick schema" (a RESOLVER.md entry carrying all 7 routing fields versus the older minimal 3-field form); clarified that `agents/` is a skillify-package surface, not a global requirement imposed on every bstack skill (stated in both SKILL.md Overview and schemas.md §4).
- 2026-06-07 — provenance: skillify's two-layer discipline — fix the first action as a deterministic script, then converge across multiple model families — was adopted from gstack (Garry Tan) and dissolved into this recipe. The borrowed philosophy lives on as function; the credit lives here, not in the skill body.
- 2026-06-07 — recipe-completeness law extended with a second contamination class — external attribution/provenance: the functional principle stays as a present-tense imperative invariant in the body; the source/credit ("inspired by X", "adopted from `<tool>`", "`<repo>`-style") moves to CHANGELOG.md. Added a contamination test, a Common Rationalizations row, and a Red Flag for it. This is the law applied to itself: the gstack attribution was removed from SKILL.md and recorded here under the new rule.
- 2026-06-07 — added the MECE law as the umbrella structural principle: a skill's internal parts must be Mutually Exclusive (each part owns exactly one responsibility, no overlap) and Collectively Exhaustive (the parts together cover the whole recipe with no gaps — the recipe-completeness law is the CE half). Added a MECE structure section and a non-MECE Red Flag.
- 2026-06-07 — v3.0.1 MECE self-audit hardening (skillify applied to itself): resolved the audit's ME contradictions and CE gaps. ME: `pipeline.md` Writer/Reviewer Task prompts no longer re-specify the frontmatter contract (had drifted to 3-key vs the canonical 5-key) — they now defer to `agents/writer.md`/`agents/reviewer.md`/`schemas.md` and own only the invocation; the Reviewer Task stops doing format-checking (a Layer-1 script job) and runs reviewer.md's quality axes; `SKILL.md ### Recipe-completeness law` no longer reproduces schemas.md §1 verbatim — schemas.md §1 is now the sole owner of the three-way-split procedure (the external-attribution rule was lifted into schemas.md §1 so the pointer is complete) while SKILL.md keeps the two operating gates; the Layer-2 gate-verdict/promotion paragraphs collapsed into the Tier-gates checklist (single owner of the pass condition); the Tier-2 "ORDERING IS LOAD-BEARING" annotation dropped (the checklist ordering and Rationalizations table carry it). CE: added an abort/roll-back subsection for abandoned promotions; bounded the consensus loop at 3 rounds with a defined user-override procedure (status: user-override + rationale + operator, invalid with zero live providers); expanded the deprecation path to create/update/move depth (status: deprecated, replacement pointer, routing/cron teardown, MAJOR bump); added the missing `§ Grader Task` invocation template + grader timeout to pipeline.md; repointed `agents/writer.md` Step 4 from a hardcoded `## Steps`/`## Output` list to schemas.md §3 anatomy; synced the frontmatter description trigger phrases with the When-to-Use list.
- 2026-06-07 — round-6 consensus follow-up (codex REVISE resolved): the deprecation and abort paths no longer instruct adding a `status` frontmatter key — that would violate the fixed 5-key contract; deprecation is now marked by a `## Deprecated` body section and an abandoned scaffold is left unregistered (no routing entry, kept on its branch) instead of a 6th key. Abort is now an explicitly user-confirmed decision with a guardrail against unilateral branch delete / PR close. The clean-start step now runs `git status` and stashes unrelated uncommitted work first, never discarding user-owned changes to reach a clean tree.
- 2026-06-13 — v3.0.3 vendor+genericize for craft-skills: removed all bstack-specific infrastructure (Hermes RESOLVER mount / skills.external_dirs, Ataraxia vault / obsidian-cli dependency, AGENTS.md §8 governance, Brain-filing Tier-2 step); replaced bstack identifiers with craft-skills throughout SKILL.md, all references/, all scripts/ docstrings/messages; routing model rewritten to reflect Claude Code plugin marketplace discovery (SKILL.md frontmatter description, no RESOLVER.md required for flat skills); compatibility field updated to claude-code, codex; protected-routing-pr-approval.md genericized; skill-pr-branch-hygiene.md personal name removed; bstack-ssot-migration.md repurposed as craft-skills plugin relocation guide.
- 2026-06-07 — jargon/unexplained terminology promoted to an explicit contamination class (third class, after functional-principle/history and external-attribution/provenance): an undefined term of art or abstract/cultural blurb in the body is a contamination — resolve by defining inline at first use or replacing with concrete technical phrasing (body), and recording the specific clarification decision and provenance in CHANGELOG.md. Added a Common Rationalizations row ("this term is well-known, no need to define it") and a Red Flag ("undefined term of art / abstract-cultural blurb / 'we call it X because…' meta-note in the body") to SKILL.md; added the jargon class to schemas.md §1 parallel to external-attribution. Applying the law to itself: this rule was developed from repeated consensus-model (gemini) findings flagging unexplained terminology across multiple rounds.
