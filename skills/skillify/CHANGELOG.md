# Changelog

- 2026-06-30 — v3.5.0: per-package CHANGELOGs had grown into multi-line bullets that were hard to scan as they stacked up → adopted a compact convention (each bullet = one line, phrased `why → what`, with `[]()` links to the skills/files it references), codified it in SKILL.md and [schemas.md](references/schemas.md), and migrated every existing CHANGELOG to the format in one pass (append-only resumes for new bullets). Additive (MINOR).
- 2026-06-30 — v3.4.0: thick skills (a waypoint + nested sub-recipes) had no blessing or validator support → blessed the layout (one waypoint + on-demand `<child>/SKILL.md`, 2-key frontmatter, no RESOLVER/manifest) and taught [validate-skill-format.py](scripts/validate-skill-format.py) `is_subrecipe()` + lighter checks (sub-recipe = name+description only; area leaves stay full packages, +5 tests), documented in [schemas.md](references/schemas.md) §1.6/§2/§4 and [topology-and-routing.md](references/topology-and-routing.md); ships with the [documents](../documents/SKILL.md) restructure. Additive (MINOR).
- 2026-06-16 — v3.2.0: whole-file review made the panel re-flag existing prose on every additive change (the flip-flop divergence behind the v3.1.0/v3.1.1 overrides) → made Layer-2 consensus diff-scoped via a `--diff-base` flag on [consensus.py](scripts/consensus.py) (reviews only added/modified lines, full SKILL.md still shipped as context, receipt gains a `scope:` line, empty diff falls back to whole-file; +6 tests). Backward-compatible (MINOR).
- 2026-06-16 — v3.1.1: the `description` carried a capability-blurb tail and the body used unexplained jargon (`SSOT`, `OMC-independent`) while governance scope was ambiguous → trimmed `description` to trigger phrases only, defined the jargon inline ("single source of truth", "orchestrator-independent"), and split governance scope (break-glass covers the skillify package itself; lifecycle work on other skills uses normal branch→PR). No behavioral change (PATCH).
- 2026-06-16 — v3.1.0: candidates were authored before anyone asked whether they belonged in craft-skills at all → added Stage 0: Admission, a pre-Harvest scope gate ([checklist.md](references/checklist.md) with five drop-questions + routed fail-actions), generalized [reviewer.md](agents/reviewer.md) into a checklist-driven judge with admission/quality modes, and wired a Tier-1 admission-receipt precondition (REJECT escalates to the human). Additive (MINOR).
- 2026-06-13 — v3.0.4: skillify still carried bstack/Hermes/Ataraxia references after being vendored → genericized every reference across SKILL.md/references/scripts/tests to craft-skills + Claude Code/Codex and fixed residual jargon leaks (`합일`→`CONVERGED`, inline `load key` definition, `우리끼리만`→`local-only`, dropped `hermes` from valid `compatibility` values, generalized the vault hand-off).
- 2026-06-07 — v3.0.2: the Hermes hook command was a literal host-absolute home path that skillify's own `validate-runtime-hygiene.py` flagged and CI failed on → replaced it with the home-relative `~/dev/GoBeromsu/bstack/.claude/hooks/protect-skillify.sh` bare +x path (Hermes expands a leading `~` under `shell=False`, portable across hosts) and renamed a test-only `token`→`grant` so it stops tripping the SECRET_ASSIGNMENT rule.
- 2026-06-07 — v3.0.2: the consensus panel (codex/gemini/claude) flagged Bash/terminal bypasses of the protection hook → hardened it — broadened the terminal denylist (command chaining, more mutating verbs, parent-dir safety net), added path normalization and a null-payload guard, reframed the Bash layer as honest best-effort with accurate token-scope wording, and documented the headless-Hermes `SKILLIFY_EDIT_TOKEN` asymmetry; regression suite 23→40 cases.
- 2026-06-07 — v3.0.2: the Hermes protection hook didn't actually fire (wrong event key, missing Hermes tool names and `.path` extraction, single-runtime deny) → made it functional (`pre_tool_use`→`pre_tool_call`, added `terminal`/`write_file`/`patch` matchers + `.path`, dual-runtime block output, broadened the deny regex to `>`/`tee`/`sed -i`) and locked the contract with [test_protect_skillify.py](tests/test_protect_skillify.py) (23 cases).
- 2026-06-07 — v2.0.0: skill format had no canonical source and carried `make-skill`/vault-stub cruft → adopted addyosmani skill-anatomy as the format SSOT (`references/skill-anatomy.md`), rewrote SKILL.md to the anatomy flow, dropped `make-skill`/vault-stub/`agent_skill_scope` language, added the flat-first placement policy, and required a per-skill `.env` (commit only `.env.example`). MAJOR.
- 2026-06-07 — skill-format verification was a non-deterministic prompt → made it a deterministic [validate-skill-format.py](scripts/validate-skill-format.py) (+ [tests](tests/test_validate_skill_format.py)) enforcing frontmatter name/description/version, a per-package CHANGELOG with a dated bullet, no `## Change Log` in SKILL.md, and no committed real `.env`.
- 2026-06-07 — the format/runtime-hygiene validators weren't enforced in CI and pointed at stale paths → wired both into GitHub CI in `--diff-base` mode (only PR-changed packages enforced) and fixed `skills/infra/skillify`→`skills/skillify`.
- 2026-06-07 — `references/` had sprawled into 11 overlapping files → consolidated to MECE (11→8): merged area-folder-migration/-hygiene/resolver-routing-audit into [topology-and-routing.md](references/topology-and-routing.md), folded runtime-hygiene-diff-mode into the PR playbook and session-review into SKILL.md, and rewrote `pipeline.md` onto the CHANGELOG.md model.
- 2026-06-06 — skill edits made before the PR flow existed had no backfill rule → clarified backfill discipline and preferred follow-up CI-fix commits on PR branches over force-push unless explicitly approved.
- 2026-06-05 — the skillify branch/merge loop was undocumented → recorded the preferred loop: focused branch/PR, return to `main`, review/merge, then pull `main` before the next skill change.
- 2026-06-05 — runtime-hygiene diff mode produced stale `HEAD` findings after local host-path cleanup → fixed it to validate the current worktree against the compare base.
- 2026-06-03 — skill governance wasn't reviewable → made skillify own skill CRUD and require every skill change to go through branch/commit/PR with a package CHANGELOG bullet.
- 2026-06-03 — runtime-hygiene regressions had no playbook pointer → linked the runtime-hygiene PR playbook from skillify's operational notes.
- 2026-06-03 — user session corrections stayed chat-only and never reached the skill → added a session review/update mode so workflow corrections patch the governing skill package instead.
- 2026-06-06 — `pipeline.md` hardcoded a bare `Ataraxia/` vault prefix → replaced it with `${ATARAXIA_VAULT_PATH}` in both the Writer and Reviewer Task Skill-Guideline paths (two-vault routing migration).
- 2026-06-07 — v3.0.0: skillify lacked a robust verification gate → added two-layer verification (Layer-1 deterministic format gate + Layer-2 orchestrator-independent consensus loop via [consensus.py](scripts/consensus.py)), 5-key frontmatter (added `allowed-tools`, `compatibility`), the embedded recipe-completeness law with Rationalizations/Red-Flags, a 3-agent roster (writer/reviewer/grader) with author-vs-eval lane separation, and repointed skill-anatomy references to [schemas.md](references/schemas.md). MAJOR.
- 2026-06-07 — the v3.0.0 pass left review gaps → reordered `description` to trigger-phrase-first, documented the convergence rebuttal as a separate `--round 2 --prior` invocation, stated the degraded-run Tier-2 policy explicitly, corrected the claude consensus role-prompt, completed [schemas.md](references/schemas.md) §4 (CHANGELOG.md/evals/agents/ rows), and switched `protect-skillify.sh` to `openssl rand -hex 16`.
- 2026-06-07 — consensus kept re-flagging unresolved `agents/` references (codex r1–r3, gemini r3) → declared `agents/` a named package part in SKILL.md and [schemas.md](references/schemas.md) §4, justified `agents/`-over-`prompts/`, removed "before anything else" from the Layer-1 heading, and disambiguated script graceful-degradation from the Tier-2 gate verdict.
- 2026-06-07 — the convergence section leaned on cultural framing and undefined terms → replaced the "Garry Tan / bstack" framing with the technical "Harvest → Prove → Cement", added inline definitions for "load key" and "thick schema", and clarified `agents/` as a skillify-package surface, not a global requirement.
- 2026-06-07 — skillify's two-layer discipline (fix the first action as a deterministic script, then converge across model families) was borrowed and needed crediting without polluting the body → recorded its provenance from gstack (Garry Tan) here in the CHANGELOG, leaving only the function in the recipe.
- 2026-06-07 — source/credit notes ("inspired by X", "adopted from `<tool>`") were contaminating SKILL.md bodies → extended the recipe-completeness law with an external-attribution/provenance contamination class (principle stays in the body, credit moves to CHANGELOG.md), added a contamination test, a Rationalizations row, and a Red Flag, and removed the gstack attribution from the body.
- 2026-06-07 — the recipe-completeness law had no umbrella structural principle → added the MECE law (parts Mutually Exclusive — one responsibility each — and Collectively Exhaustive — recipe-completeness is the CE half), with a MECE structure section and a non-MECE Red Flag.
- 2026-06-07 — v3.0.1: a MECE self-audit found ME contradictions and CE gaps in skillify itself → resolved them — `pipeline.md` Writer/Reviewer prompts defer the frontmatter contract to [schemas.md](references/schemas.md)/agents instead of restating it, schemas.md §1 became the sole owner of the three-way-split, and added abort/roll-back, a 3-round consensus bound with user-override, a create/update/move deprecation path, and the missing `§ Grader Task` template.
- 2026-06-07 — the deprecation/abort paths told authors to add a `status` frontmatter key, breaking the fixed 5-key contract → marked deprecation with a `## Deprecated` body section and left abandoned scaffolds unregistered instead, made abort an explicitly user-confirmed step, and made clean-start run `git status` + stash unrelated work first.
- 2026-06-13 — v3.0.3: skillify was being vendored into craft-skills but still bound to bstack infrastructure → removed bstack-specific infra (Hermes RESOLVER mount, Ataraxia vault/obsidian-cli, Brain-filing Tier-2 step), rewrote routing for Claude Code plugin-marketplace discovery (no RESOLVER.md for flat skills), set `compatibility: claude-code, codex`, and genericized all references/scripts.
- 2026-06-07 — gemini consensus kept flagging unexplained terminology across rounds → promoted jargon/unexplained-terminology to a third contamination class (define inline at first use or replace with concrete phrasing in the body; record the decision in CHANGELOG.md), with a Rationalizations row, a Red Flag, and a [schemas.md](references/schemas.md) §1 entry.
- 2026-06-17 — v3.3.0: skill `name` fields drifted into long description-restating phrases → added a compact-name style contract to [schemas.md](references/schemas.md) §2 (terse one-concept handle, ≤2 tokens, banned `-skill`/`how-to-`/`my-` affixes and verb-object phrases), surfaced it in SKILL.md (pointer, Rationalizations row, Red Flag), and filled the Trigger-Fit CE gap in [reviewer.md](agents/reviewer.md) Step 3.
