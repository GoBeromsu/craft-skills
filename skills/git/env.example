# Tailscale remote-execution extension (optional).
# Set to a Tailscale hostname or user@host to enable remote worktree mode.
# Leave unset (or delete this file) for local-only operation.
CRAFT_WT_REMOTE_HOST=<tailscale-host-or-user@host>
