# Tool preflight

Run this reference before a recipe depends on one of these mutable command-line tools.
It is not a host inventory, an upstream SSOT, or a CHANGELOG receipt schema.
Installed versions and host upgrades belong in historical CHANGELOG or the parent ledger, not here.

Authoring create/update of a package that depends on a related official skill or CLI follows the `skillify` skill's contract §10: current matching versions are a verified no-op; stale related dependencies are actually updated through the official channel, then resulting versions are read back and affected siblings are repaired; check-only detection or a failed update leaves the run incomplete. Informational read-only requests do not authorize installation. Unrelated tools stay untouched.

| Tool | What it is | Official source | Install location probe | Version probe | Support boundary | Known incompatibilities | Fallback when unavailable or probe fails |
| --- | --- | --- | --- | --- | --- | --- | --- |
| `obsidian-cli` | Yakitrak community CLI for vault operations. | [Yakitrak/obsidian-cli](https://github.com/Yakitrak/obsidian-cli/releases) | `command -v obsidian-cli` | `obsidian-cli --version` | Support only documented Yakitrak CLI commands validated for the installed version. | Do not confuse it with the `obsidian` Obsidian.app CLI binary. | Use the Obsidian.app `obsidian` CLI only for commands it owns; otherwise read the vault through the plain filesystem and note the degraded path. |
| `obsidian` | Obsidian.app official CLI with a separate command surface and binary from `obsidian-cli`. | [Obsidian CLI](https://help.obsidian.md/cli) | `command -v obsidian` | `obsidian version` | Support documented Obsidian.app CLI commands available in the installed app. | `obsidian --version` prints help on this installation; use `obsidian version` for the version. The CLI answers through the running app and can print nothing while exiting 0 when that bridge is briefly unavailable, so an empty result is indeterminate, not zero. | Use `obsidian-cli` only for commands it owns; otherwise read the vault through the plain filesystem and note the degraded path. |
| `qmd` | Quick Markdown Search CLI from the `@tobilu/qmd` npm package. | [@tobilu/qmd](https://www.npmjs.com/package/@tobilu/qmd) | `command -v qmd` | `qmd --version` | Support documented QMD commands validated for the installed version. | Node 26 ABI failures require Node 24. | For a missing tool or Node ABI error, run `nvm use 24` and retry; if that fails, use `rg` over the vault and note that indexed/semantic search is unavailable. |
| `aside` | Browser agent that executes natural-language tasks in a real browser. Native app and CLI are separate dependencies. | [Aside](https://aside.com/). Product controls and skill installs belong to unmodified official `aside-browser` plus current `aside guide`, `aside guide repl`, and installed help. | `command -v aside` | `aside --version` | Support the installed CLI's current native help. | `aside --update` mutates installation state and is never a read-only probe. Root `--session` is version-scoped evidence of a removed flag; current native guide/help win that conflict. Official web docs remain useful elsewhere. | Stop web enrichment and report `skipped`; never replace Aside with `curl` scraping, another managed browser, or a forced user-browser restart. |
| `defuddle` | CLI that extracts clean Markdown or metadata from web pages. | [kepano/defuddle](https://github.com/kepano/defuddle/releases) | `command -v defuddle` | `defuddle --version` | Support documented `parse` capabilities validated for the installed version. | Use a browser for JavaScript-heavy pages. | Report extraction as `unavailable`; `curl -sL` plus readability is not a substitute. |
| `gh` | GitHub CLI. | [GitHub CLI releases](https://github.com/cli/cli/releases) | `command -v gh` | `gh --version` | Support documented command surfaces available in the installed release. | Authentication and repository permissions bound supported operations. | Stop GitHub API or PR operations and report `unavailable`; do not substitute undocumented HTTP calls. |
| `hermes` | Hermes Agent CLI. | [NousResearch/hermes-agent](https://github.com/NousResearch/hermes-agent/releases) | `command -v hermes` | `hermes --version` | Support documented installed CLI subcommands only. | Do not infer cron behavior from an older installed surface. | Stop: Hermes is the runtime. |
| `gjc` | Gajae Code CLI. | [Gajae Code](https://github.com/Yeachan-Heo/gajae-code) | `command -v gjc` | `gjc --version` | Support the installed CLI's documented command surface only. | Use `gjc skills`, not the nonexistent `gjc skill` command. | Stop: GJC is the runtime. |

## Preflight recipe

1. Run `command -v <tool>` and that tool's exact version probe before declaring it unavailable.
   A single empty or failing probe is not proof of absence: shell and app-bridge flakiness produce empty output with a zero exit code. Confirm with a second, different probe — a direct install-path check or the tool's own help — and report which probe settled it.
2. Do not run a mutating subcommand such as `--update` or `install` as a probe.
   A create/update authoring run still follows the `skillify` skill's contract §10 when a related official skill or CLI is stale. A read-only informational request does not authorize installation.
3. Treat an unavailable, ambiguous, or unsupported tool as a boundary and report it rather than inventing a fallback.
4. Verify the affected recipe behavior after a probe, update, or sibling repair — a real command, error, or effect on the dependent path — not a generated eval corpus or a CHANGELOG receipt token.
