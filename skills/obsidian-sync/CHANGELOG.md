- 2026-08-27 — v3.0.0: moved from `skills/obsidian-sync` (flat) to `skills/obsidian/sync` (leaf of the new `obsidian` area) as part of the domain-topology reorganization; `name` changed `obsidian-sync` → `sync` to match the new load key `obsidian/sync` (**breaking**: the old bare `obsidian-sync` load key no longer resolves).
- 2026-07-26 (b) — remove two internally-contradictory procedures that survived the first pass. `references/remote-m1-ops.md` still presented `scp <cjs> && pm2 reload ~/.config/pm2/ob-sync-ataraxia.config.cjs && pm2 save` as the normal reload path (replaced with an explicit FORBIDDEN block: reloading from the drifted `.cjs` retargets sync to a dead path and the trailing `pm2 save` writes that state into `dump.pm2`, killing sync across reboots). `references/pm2-daemon.md` claimed `pm2 restart <name> --update-env` picks up ecosystem-file edits — it does not; restart-by-name re-specifies from `dump.pm2`, so the correct procedure is `pm2 delete` + re-`start` with explicit args + `pm2 save`. Also corrects the remaining `ssh m1-pro` → `ssh m1-pro-raw` instructions in `SKILL.md` (description + Phase-2 prefix). Found by the M1 hermes agents (sari, xia) reading the propagated docs.
- 2026-07-26 — document the measured M1-pro/M3 `${OBSIDIAN_VAULT_PATH}` divergence and case-sensitive `ob` config keys, the pm2 boot-chain SSOT (`~/Library/LaunchAgents/pm2.beomsu.plist` → `pm2 resurrect` → `~/.pm2/dump.pm2`) with a warning against restarting via the `.cjs` ecosystem file, and the `ssh m1-pro-raw` connection requirement / `~/Documents` iCloud-walk hazard on M1. Bump to v2.0.1.
- 2026-07-12 — consolidate the two `obsidian-sync` skills into one flat skill (v2.0.0): the superset from `obsidian-tools/obsidian-sync` (Preflight→Stage→Verify→Promote bootstrap, silent-deletion incident recovery, pm2/tailscale daily ops, 7 references) absorbs the flat daemon-ops skill's expired-subscription auth crash-loop and macOS TCC file-visibility triage; adds `codex` compatibility and the `/obsidian-sync` + daemon-triage trigger phrases. The `obsidian-tools` area is dissolved — its generic Obsidian craft (bases, canvas, cli, clipper, doctor, markdown, mermaid) and `defuddle` moved to the craft-skills repo.
- 2026-07-09 — create obsidian-sync skill: headless `ob` sync-daemon status check, pull-only sentinel safe-reconcile runbook, and auth/crash-loop triage; owns sync-daemon operations distinct from obsidian-tools (app mechanics). Provenance: distilled from a live m1-pro headless-sync recovery (expired-subscription crash-loop + macOS TCC file-visibility edge). (v1.0.0)
- 2026-08-27 — v3.0.1: hermes skills_guard compatibility: reworded 'each host' wording and .claude/settings.json reference; no behaviour change.
- 2026-08-27 — v3.0.2: hermes skills_guard compatibility (round 2, leaf-accurate scan): the new per-leaf scanner now fetches this package's full reference bundle and surfaced further false positives — reworded 3 `.claude/settings.json` self-description mentions in references/defense-stack.md, 2 `~/.ssh/config` mentions in references/remote-m1-ops.md (now `${HOME}/.ssh/config`), and 3 uses of the literal word "sudo" in references/pm2-daemon.md describing a command that itself prints a sudo suggestion; no behaviour change.
- 2026-08-28 — v4.0.0: moved the active Obsidian sync skill from GoBeromsu/bstack@3e0672c into craft-skills as `obsidian-sync`, replacing the deprecated stub and restoring craft-skills as the reusable Obsidian-craft owner.
