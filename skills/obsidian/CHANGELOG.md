# Changelog

- 2026-08-28 — v1.0.0: eight `obsidian-*` packages fragmented one cohesive tool surface → consolidate Markdown, Bases, Canvas, Mermaid, CLI, Clipper, doctor, and headless Sync into one thick `obsidian` skill with selectively loaded sub-recipes; Grok 4.6 `xhigh` baseline-delta evals rated all three scenarios better and trigger-fit passed after four correction rounds. Provenance: moved from the active craft-skills `obsidian-*` packages and bstack `obsidian/*` lineage at `3e0672c`.
- 2026-08-28 — mutable Obsidian CLI, app, and plugin behavior needs grounded handling → v1.1.0 adds an official-docs-first evidence rule with conflict disclosure and safe unknowns.
- 2026-08-28 — v1.1.1: release updates can invalidate Obsidian recipe assumptions → record official sources and safely observed versions, then rerun affected sub-recipe evaluations.
- 2026-08-28 — v1.1.2: changed runtime form could leave lifecycle work incomplete → probe/release evidence now triggers official-docs recheck, affected package evals, recipe update as needed, then version bump and CHANGELOG.
- 2026-08-30 — v1.2.0: the Bases sub-recipe could not answer how to embed a specific view, which layouts a given Obsidian version has, or how to extract a base's rows → add an embed section (`![[Base#View]]` selector, and the rename hazard it creates), a toolbar extraction section (search/limit/copy/CSV export), and version-gated layout availability; restate the single-`groupBy` limit as a current engine constraint verified through 1.12 rather than a 1.10 artifact. Provenance: authored while building bstack `hermes/hermes-cron`, verified against Obsidian 1.12.7 and the official Bases syntax/Views documentation.
