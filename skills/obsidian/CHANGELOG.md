# Changelog

- 2026-09-20 — v1.3.0: the package called the third-party Yakitrak `obsidian-cli` "the Obsidian CLI", hiding the official Obsidian.app binary and its `backlinks`/`links`/`unresolved`/`property:*`/`sync:*` commands → [`cli.md`](references/cli.md) is rebuilt around the official CLI with Yakitrak scoped to a labelled headless fallback, [`doctor.md`](references/doctor.md) and [`doctor-pipeline.md`](references/doctor-pipeline.md) drop the mislabel and the invented `plugin:list`/`note:edit`/`note:delete`/`obsidian status`, [`sync.md`](references/sync.md) hands read-only Sync state to the CLI recipe, and three field-derived guards land: empty output with exit code 0 is indeterminate and must be retried, no tool is declared unavailable on one probe, and every CLI claim names its binary. Evidence: installed `obsidian help` audited so 0 of 38 referenced commands are invented; `plugins:enabled` observed returning 23 lines, then empty, then 23 lines with the app running; `search`/`search:context` empty on 5+ attempts for a present term, so documented as unconfirmed. verified_against: obsidian@1.14.2 (installer 1.13.4), obsidian-cli@0.2.3.

- 2026-09-10 — v1.2.3: mandatory obsolete default-vault probing blocked the installed community NotesMD CLI → verify supported registry lookup and the explicit target mapping without changing defaults; distinguish official app commands and headless prerequisites. Evidence: notesmd-cli v0.3.7 version/root help/list-vaults help and upstream documentation; no live note operation or app-index verification claimed.

- 2026-08-28 — v1.0.0: eight `obsidian-*` packages fragmented one cohesive tool surface → consolidate Markdown, Bases, Canvas, Mermaid, CLI, Clipper, doctor, and headless Sync into one thick `obsidian` skill with selectively loaded sub-recipes; Grok 4.6 `xhigh` baseline-delta evals rated all three scenarios better and trigger-fit passed after four correction rounds. Provenance: moved from the active craft-skills `obsidian-*` packages and bstack `obsidian/*` lineage at `3e0672c`.
- 2026-08-28 — mutable Obsidian CLI, app, and plugin behavior needs grounded handling → v1.1.0 adds an official-docs-first evidence rule with conflict disclosure and safe unknowns.
- 2026-08-28 — v1.1.1: release updates can invalidate Obsidian recipe assumptions → record official sources and safely observed versions, then rerun affected sub-recipe evaluations.
- 2026-08-28 — v1.1.2: changed runtime form could leave lifecycle work incomplete → probe/release evidence now triggers official-docs recheck, affected package evals, recipe update as needed, then version bump and CHANGELOG.
- 2026-08-30 — v1.2.0: the Bases sub-recipe could not answer how to embed a specific view, which layouts a given Obsidian version has, or how to extract a base's rows → add an embed section (`![[Base#View]]` selector, and the rename hazard it creates), a toolbar extraction section (search/limit/copy/CSV export), and version-gated layout availability; restate the single-`groupBy` limit as a current engine constraint verified through 1.12 rather than a 1.10 artifact. Provenance: authored while building bstack `hermes/hermes-cron`, verified against Obsidian 1.12.7 and the official Bases syntax/Views documentation.
- 2026-09-02 — v1.2.1: community and official Obsidian CLI binaries could be confused during runtime checks → link the shared tool preflight reference and collapse the contract into `## Output contract` with its stop condition. verified_against: obsidian-cli@0.2.3, obsidian@1.13.7.
- 2026-09-03 — v1.2.2: a cross-package link climbed out of the package, which the Hermes tap fetcher treats as traversal and refuses to install → name the owning skill and file in prose; add the Output contract and eval corpus the contract requires.
- 2026-09-20 — v1.3.1: trimming the v1.3.0 description under the 1024-char cap silently dropped the `obsidian sync status` and `pull-only로 맞춰줘` trigger phrases plus the `daemon restart` anchor, and `obsidian sync status` is an asserted `should_trigger` prompt, so the corpus demanded a route whose discovery anchor no longer existed → restore all six quoted trigger phrases and the daemon anchor at 1021 chars by trimming non-routing detail instead, keeping the change MINOR under the [version rubric](../skillify/references/contract.md#8-version-bump-rubric) rather than the MAJOR a removed trigger would force. Evidence: quoted-phrase diff of the description against pre-change base `0b33a79` reports zero removed phrases; format validator 0 errors.
- 2026-09-21 — v2.0.0: thick local Markdown, Bases, Canvas, and CLI manuals duplicated unchanged native skills → compose `obsidian-markdown`, `obsidian-bases`, `json-canvas`, and `obsidian-cli` by package identity; native skills now own format, schema, and CLI mechanics while local references retain only target-vault/native-surface authorization, missing-capability reporting, [exact non-Markdown destination readback](references/markdown.md#non-markdown-targets-and-destination-readback), [Canvas renderer evidence](references/markdown-embeds.md#embed-canvas), app/rendering coordination, plugin/renderer/Sync boundaries, and observed supervisor plus separate Git-backup behavior. Retire copied CLI/fallback mechanics and the redundant Canvas reference. Preserve the distinction between authorized content roundtrip, scoped restart recovery, remote-backup readback, pairing, and process status. Provenance: official [Internal links](https://help.obsidian.md/links) and [Embed files](https://help.obsidian.md/embeds), compared with installed official skills without modifying originals. Parent-reported probes: verified_against: obsidian@1.14.2 (installer 1.12.7), ob@0.0.14 (installed/runtime/npm latest agree); public GitHub release endpoint v1.13.8 differs from the installed app, so no downgrade or newest-release claim; live link/rendering and Sync/backup effects remain unverified by these authoring edits.
- 2026-09-22 — v2.1.0: PR138 supplied a reusable note-grounded Excalidraw route → add [`visualize.md`](references/visualize.md) for API admission, provenance, exact-target additive persistence, reload, linking, and rendering boundaries; carry a caller-known pre-launch nonce through pending, terminal, and polling states, reject blank or stale launches without replay, and leave CLI argv and shell grammar to native `obsidian-cli`. Provenance: adapted [PR138](https://github.com/GoBeromsu/craft-skills/pull/138).
- 2026-09-24 — v2.2.0: an agent following the EA-workbench-first `visualize.md` took very long and corrupted an existing drawing (dropped an element, rewired an arrow binding) building through the plugin API, while writing the `.excalidraw.md`/`.canvas` file directly is faster and has no live-scene state to corrupt → rewrite [`visualize.md`](references/visualize.md) around direct file authoring for both Excalidraw and Canvas (format-choice rule, file anatomy, element essentials, generate → validate → render-QA loop, safety rules), add [`scripts/excalidraw_scene.py`](scripts/excalidraw_scene.py) (stdlib-only `Scene` builder plus a standalone `check()` for id/binding/overlap defects), demote the former EA-workbench/async-guard/persistence content to [`references/ea-workbench.md`](references/ea-workbench.md) as an explicit exception for live edits on an open drawing or plugin-file-store embeds, and revise the SKILL.md routing row, requirements, and anti-pattern line so Canvas is a legitimate `visualize` choice rather than a banned substitution. Evidence: `excalidraw_scene.py`'s `__main__` self-check builds and validates a 3-box demo scene and round-trips it through `write_excalidraw_md`, asserting zero `check()` defects.
