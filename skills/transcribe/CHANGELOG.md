# Changelog

- 2026-07-26 — v1.0.0: a real transcription session hit local-engine detection and credential-check pitfalls that were worth codifying → added the transcribe skill covering ffmpeg preprocessing, mlx_whisper-first engine selection, diarization-honesty rules, cloud-credential verification, and Obsidian note output.
- 2026-07-26 — v1.1.0: a follow-up ElevenLabs Scribe attempt (blocked by insufficient credits) and further mlx_whisper runs surfaced the credit-math ceiling, a multi-file output-overwrite bug, and a hallucination-repeat loop → added chunking with timestamp-offset correction, the `mlx_whisper` anti-loop flags and mandatory uniqueness quality gate, the ElevenLabs credit-math precheck with a hard no-payment/no-upgrade rule, downgraded ElevenLabs from a general fallback to a credit-gated conditional path with no verified diarization output, and a Korean technical-jargon misrecognition warning.
- 2026-07-26 — v1.2.0: after the user funded an ElevenLabs Starter plan directly, a real 15:20 two-speaker Korean recording was transcribed and diarization was verified end-to-end (`[Speaker N]` tags, evidence-based identity mapping) → rewrote the diarization section from "no verified path" to a verified procedure, flipped the engine table to route to ElevenLabs first whenever speaker separation is needed, corrected the credit-math section with the real post-transcription cost (5,063 credits vs. the ~12,785-credit pre-upload estimate, now framed as a ceiling not a forecast), promoted the jargon-misrecognition warning to an engine-agnostic rule with ElevenLabs-specific examples, and documented the report-shortfall-and-stop → user-funds → retry flow as a validated pattern while keeping the no-payment rule for the agent itself.
